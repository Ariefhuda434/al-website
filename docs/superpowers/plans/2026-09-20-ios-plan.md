# iOS Style + Bio + Dashboard — Implementation Plan

**Goal:** Mengubah portfolio ke iOS style, bio baru, dan menambahkan dashboard login.

**Tasks:**
1. Perbarui `page.tsx` — nama, bio, iOS glassmorphism cards
2. Perbarui `globals.css` — palet iOS pink
3. Buat halaman `/admin` dengan Firebase Auth
4. Perbarui `layout.tsx` — navigasi
5. Perbarui `MusicPlayer` — tetap ada
